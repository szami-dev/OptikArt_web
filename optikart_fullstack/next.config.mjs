/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ide jöhetnek Next.js beállítások.
  // (Server/Client futás szempontból ez csak build/időben számít.)
};

export default nextConfig;

